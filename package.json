{
    "name": "aws-recommender",
    "version": "1.0.0",
    "description": "aws-recommender-service",
    "main": "dist/index.js",
    "scripts": {
        "build": "rimraf -g 'node_modules' && tsc",
        "test": "jest --coverage"
    },
    "dependencies": {
        "@types/aws-lambda": "^8.10.130",
        "node-fetch": "^2.6.1",
        "typescript": "^4.5.4"
    },
    "devDependencies": {
        "@types/jest": "^29.5.10",
        "jest": "^29.7.0",
        "rimraf": "^5.0.5",
        "ts-jest": "^29.1.1"
    }
}
